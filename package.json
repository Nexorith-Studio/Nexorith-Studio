{
  "name": "nexorith",
  "version": "1.0.0",
  "description": "Nexorith Fullstack Workspace",
  "private": true,
  "scripts": {
    "dev:frontend": "npm run dev --prefix frontend",
    "dev:backend": "npm run dev --prefix backend",
    "build": "npm run build --prefix frontend",
    "start": "npm run start --prefix frontend"
  },
  "dependencies": {
    "next": "^16.2.1"
  }
}
